"""PySide6 GUI package for Bulk Media Downloader.

Importing this package (or its modules) requires PySide6. The core package must
never import from here; only ``app.run_gui`` and these modules touch Qt.
"""
